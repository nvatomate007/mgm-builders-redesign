# MGM Builders — Environment Variables

# Non-production previews default to an expiring controlled sink embedded in
# api/contact.ts. Before any approved production cutover, configure a verified
# HTTPS delivery endpoint and remove the preview-only fallback and form notice.
FORM_DELIVERY_URL=
FORM_DELIVERY_MODE=approved-production-recipient
